#include <iostream>
using namespace std;

int main() {
    int x = 5;
    int y = 3;
    if (x >= y) {
        cout << "x is greater than or equal to y." << endl;
    } else {
        cout << "x is not greater than or equal to y." << endl;
    }
    return 0;
}
