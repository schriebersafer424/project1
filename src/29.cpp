#include <iostream>

int main() {
    int x = 5;
    int y = 3;

    if (x > y) {
        std::cout << "x is greater than y." << std::endl;
    } else {
        std::cout << "y is greater than x." << std::endl;
    }

    return 0;
}
